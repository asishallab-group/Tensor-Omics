# TOX Flyer Prototype

To test the flyer, run `python3 -m http.server 8080` to serve the files of this directory.
Then open http://localhost:8080/index.html in your browser.

The config object is available in dev tools, so just open the console and customize whatever you want.
See `./config.js` for supported attributes.
